name = "wvm-poll"
main = "worker.js"
compatibility_date = "2024-11-01"

# Bind a KV namespace (create it once: `wrangler kv namespace create POLL`) and paste its id below.
[[kv_namespaces]]
binding = "POLL"
id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID"
