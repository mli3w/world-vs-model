numpy>=1.24
pandas>=2.0
requests>=2.31
pytest>=7.4
pillow>=10.0    # for src/make_chart.py + the other shareable social charts
matplotlib>=3.7  # only for src/make_zk_formula.py + src/make_reliability.py (research-flavour share assets)
